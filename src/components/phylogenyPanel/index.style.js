import styled from "styled-components";

const Wrapper = styled.div`
  .ant-wrapper {
    background: white;
    padding: 0px;
  }
  .tooltip-box {
    .header {
      font-size: 16px;
      font-weight: 600;
      text-align: left;
    }
    table.content {
      td {
        text-align: left;
        padding: 3px;
        padding-top: 2px;
        padding-bottom: 2px;
        min-width: 120px;
      }
    }
  }
`;

export default Wrapper;
