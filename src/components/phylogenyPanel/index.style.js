import styled from "styled-components";

const Wrapper = styled.div`
  .ant-wrapper {
    background: white;
    padding: 0px;
  }
  .tooltip-box {
    .header {
      margin: 10px 0px;
      font-size: 12px;
      font-weight: 600;
    }
  }
`;

export default Wrapper;
