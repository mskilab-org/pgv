import styled from "styled-components";

const Wrapper = styled.div`
  .site-page-header {
    background: white;
    padding: 0px;
    margin: 0px 24px;
    .site-page-content {
      margin-bottom: 24px;
    }
    .aligned-center {
      display: inline-flex;
      align-items: center;
    }
  }
`;

export default Wrapper;
