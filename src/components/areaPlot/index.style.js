import styled from "styled-components";

const Wrapper = styled.div`
  .ant-wrapper {
    background: white;
    padding: 0px;
    min-height: 400px;
  }
  svg.plot-container {
    position: absolute;
    top: 0px;
    user-select: none;
  }
`;

export default Wrapper;
